{
  "name": "faix-deeplink-server",
  "version": "2.0.0",
  "description": "FAI-X Self-hosted Deferred Deep Linking Server with Product Sharing",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "clean": "rm -rf data/*.json && mkdir -p data && echo '[]' > data/referrals.json",
    "test": "echo \"Tests coming soon\" && exit 0"
  },
  "keywords": [
    "deep-link",
    "deferred-deep-link",
    "universal-links",
    "app-links",
    "mobile-app",
    "product-sharing"
  ],
  "author": "FAI-X Team",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.19.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
